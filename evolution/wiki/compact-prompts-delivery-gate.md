# Frozen proposal before runtime edits

Baseline: 6cf43d758d6f99aa051153edea67d5ac533acfe7.
Scope: keep execution/retrieval topology and manual-only activation unchanged.
1. Shorten runtime root and leaves; remove maintainer/evolution detail from everyday context.
2. Explicitly distinguish known location from sufficient evidence: traverse each immediate child without forcing redundant discovery queries.
3. Preserve required verification; do not interpret a cheap check as permission to omit material gates or justified flaky-test repetition.
4. Observe completed tool events and unknown usage explicitly. Never count path mentions as policy reads.
5. Share frozen, dependency-enabled execution across axes; retire the active global monkey-patch entry.
6. Provide an engineering release gate and labelled planning targets, not fabricated model data.

Validation frozen: prompt-boundary checks, positive/adversarial transcript fixtures, identical baseline/candidate evaluator regression, missing/duplicate/stale/contaminated matrix rejection, actual runner cache integration, full CI tests. Code-delivery fixtures must have independent executable oracles with known-bad mutations rejected before model runs. No published historical result rewrite. No score-based runtime wording.

Model experiments: initial n=1 screen, frozen triad n=3 with all required providers prewarmed identically; setup costs excluded. Current environment has neither model credentials nor provider executables. Any proposed percentages remain goals, never measured rows or confidence claims.

## Implemented candidate

No topology change or automatic manual mode was added. The frozen hypothesis led
to compact runtime prompts, an immediate-child bridge for already-located but
insufficient evidence, and one active runner instead of historical monkey-patches.
Typed telemetry, raw receipts, executable public code fixtures and a complete
engineering gate were implemented. Deterministic validation is recorded by CI.
No model run was available; this is not a promoted release. Targets are not
results and safety/quality cannot be traded for lower reported cost.
