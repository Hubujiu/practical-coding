# Retrieval evidence and measurement integrity — 2026-09-04

Base: `942f0aa17bdac381ad50c544683c88e6320e6cf1`.

## Frozen hypothesis before edits

Keep the execution and Retrieval topology unchanged. A small claim/stop/
freshness rule at the Retrieval root may reduce repeated discovery without
losing necessary evidence. This is an experimental wording candidate, not a
quality-qualified promotion. Do not restore execution-state, automatic Decision,
or provider-named routing.

The harness must first stop accepting stale cells and unsupported minimum-stage
claims. Reproduced on the pinned original analysis module: missing lower ceiling
reported R1; indeterminate lower ceiling reported R0; setup-contaminated result
reported NONE; duplicate repetition accepted; one pass plus two timeouts reported
100% pass rate. These are evaluator defects, not measured model improvements.

## Validation frozen before edits

Use deterministic counterexamples for each measurement defect, positive complete
matrices, cache identity mutations, and CLI failure behavior. Preserve all
historical result files and original real-repository tasks/oracles. Run an n=1
dependency-enabled current-only screen, then only a frozen candidate receives
the complete n=3 adaptive/baseline/no-skill and Retrieval-ceiling matrix.
Setup, downloads, indexing, dependency resolution, and first builds remain
unmeasured; absent providers must abort rather than select a fallback profile.

Maximum three attributable iterations; revert the wording change if paired
quality regresses or extra evidence/output cost has no compensating benefit.
No structural node is added, removed, renamed, or promoted by harness tests.

## Recorded outcome

The frozen evaluator checks pass 13/13 versus 4/13 on the exact original module;
41 focused unit tests pass. No model/provider benchmark ran because the required
binaries are absent. The wording remains an unpromoted experimental candidate.
See `benchmarks/RETRIEVAL_INTEGRITY.md` for commands, scope, and telemetry limits.
