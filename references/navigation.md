# Navigation

**Concern:** repository topology only

**Output:** the smallest bounded area in which Retrieval should begin

Enter only when the repository area is unresolved, not merely a filename. Read the repository's own map: manifests, workspace/module declarations, build files, or maintained architecture notes. Identify the packages/services/layers that can own the request and return their paths with the evidence for that boundary.

Do not inventory files, rank semantic matches, expand evidence, trace graphs, or select tools. Exclude unrelated generated/vendor/history areas unless the task needs them. Skip Navigation once scope is known; continue through `references/retrieval/SKILL.md` inside that scope. A read-only map is not an implementation plan.
