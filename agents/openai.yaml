interface:
  display_name: "Practical Coding"
  short_description: "Lean coding rules with optional upstream code intelligence"
  default_prompt: "Use $practical-coding and load only the modules this task needs."
policy:
  allow_implicit_invocation: true
