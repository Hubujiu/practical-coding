interface:
  display_name: "Practical Coding"
  short_description: "Small correct changes with event routing and bounded retrieval"
  default_prompt: "Use $practical-coding. Apply the Core, route only a present unresolved Debugging, Decision, or Implementation event, and use the cheapest sufficient retrieval capability. Load at most one reasoning reference for the current event."
policy:
  allow_implicit_invocation: true
