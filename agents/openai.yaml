interface:
  display_name: "Practical Coding"
  short_description: "Lean coding rules loaded only when needed"
  default_prompt: "Use the practical-coding skill and load only the modules this task needs."
policy:
  allow_implicit_invocation: true
