interface:
  display_name: "Practical Coding"
  short_description: "Small correct changes with adaptive code retrieval"
  default_prompt: "Use $practical-coding. Keep well-specified work on the Direct Path, load at most one reasoning reference for an unresolved blocker, and retrieve code through the cheapest sufficient available capability."
policy:
  allow_implicit_invocation: true
